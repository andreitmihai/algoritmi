#include <iostream>

using namespace std;

int main()
{
    int a, b, aux, n;
    cin>>n;
    cin>>a
    for(int i=2; i<=n; i++)
    {
        cin>>b
        while(a!=b)
            if(a>b) a-=b;
            else    b-=a;
    }
    cout<<"cmmdc:"<<a;
    return 0
}

//Palindroame

#include <iostream>

using namespace std;
{
  int n, x, ogl, aux;
  cout<<"n="; cin>>n;
  for(int i=1; i<=n; i++)
  {
      cin>>x; aux=x;
      ogl=0
      while(x)
      {
          ogl=ogl*10+x%10;
          x/=10;
      }
      if(ogl==aux) cout<<aux<<" ";
  }
  return 0 
}

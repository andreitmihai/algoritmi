//minimul a 2 nr.

using namespace std 
int main()
{
  int x, maxi, mini;
  cin>>n;
  cin>>x
  maxi=mini=x;
  for(int i=2, i<=m; i++)
  {
     cin>>x;
     if(x>maxi) maxi=x
     if(x<mini) mini=x
     
  }
  cout<<maxi<<" "<<mini;
  return 0;
}
